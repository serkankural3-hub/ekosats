rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // GELİŞTİRME MODU - Test için tüm erişimlere izin ver
    // PRODUCTION'da bu kuralları kapat!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
